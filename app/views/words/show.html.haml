%h1
  = @word.text
%table
  %thead
    %th Title
    %th href
    %th hn_id
    %th created_at
  %tbody
    - @word.entries.sorted.each do |entry|
      %tr
        %td= entry.title
        %td= link_to entry.href
        %td= entry.hn_id
        %td= entry.created_at

